{
  "name": "dsh-cursorkit",
  "version": "2.0.0",
  "private": true,
  "description": "Cursor 同款 AI IDE（VSCode 扩展 + dsh agent 内核）",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.20.1",
    "typescript": "^5.7.3",
    "vitest": "^2.1.9"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "test:run": "pnpm -r test:run",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "build:extension": "pnpm --filter @dsh-cursorkit/vscode build"
  }
}
