{
  "name": "dsh-cursorkit",
  "version": "0.1.0",
  "private": true,
  "description": "A Claude Desktop / Codex Desktop style desktop client for DeepSeek Harness (dsh)",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "test:run": "pnpm -r test:run",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "dev:desktop": "pnpm --filter @dsh-cursorkit/desktop dev"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^2.1.9"
  }
}
